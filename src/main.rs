extern crate core;

mod accumulator;
mod commitment;
mod bbs;

use std::ops::{Add, Sub};
use bls12_381::*;
use rand::prelude::*;
use std::collections::BTreeMap;
use crate::accumulator::{AccNa, random_scalar};

fn main() {

}
